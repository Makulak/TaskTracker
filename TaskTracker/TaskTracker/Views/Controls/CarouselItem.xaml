<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xForms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:resources="clr-namespace:TaskTracker.Resources;assembly=TaskTracker"
             xmlns:popupLayout="clr-namespace:Syncfusion.XForms.PopupLayout;assembly=Syncfusion.SfPopupLayout.XForms"
             x:Class="TaskTracker.Views.Controls.CarouselItem"
             BindingContextChanged="CarouselItem_OnBindingContextChanged">
    <ContentView.Content>

        <popupLayout:SfPopupLayout x:Name="AddNewTaskPopup">

            <popupLayout:SfPopupLayout.Content>

                <Grid Padding="20,0,20,0">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="45"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Frame x:Name="Header" Padding="1" BackgroundColor="Transparent">
                            <StackLayout Orientation="Horizontal" >
                                <Image IsVisible="False" x:Name="DeleteImage" Source="delete.xml"/>
                                <Label TextColor="Black" HorizontalTextAlignment="Center" Text="{Binding Name}" FontAttributes="Bold" FontSize="Medium" VerticalOptions="Center" HorizontalOptions="Center"/>
                            </StackLayout>
                        </Frame>
                    </Grid>

                    <xForms:SfListView Grid.Row="1" x:Name="ListView" BackgroundColor="#C0E0E0E0" SelectionMode="None"
                                       ItemSize="80" ItemTapped="SfListView_OnItemTapped" ItemDragging="SfListView_OnItemDragging" 
                                       ItemsSource="{Binding TaskCollection, UpdateSourceEventName=PropertyChanged, Mode=TwoWay}" 
                                       DragStartMode="OnHold" ItemSpacing="0,0,0,5" >

                        <xForms:SfListView.ItemTemplate>
                            <DataTemplate>
                                <FlexLayout Margin="5,0,0,5" BackgroundColor="#EEEEEE" AlignItems="Start" JustifyContent="SpaceBetween" Direction="Column">
                                    <Label Text="{Binding Name}" TextColor="Black"/>

                                    <Frame HeightRequest="30" WidthRequest="30" CornerRadius="15" Padding="1" BackgroundColor="LightBlue">
                                        <Image Scale="0.7" Source="{Binding AssignedUser.Image}"/>
                                    </Frame>
                                </FlexLayout>
                            </DataTemplate>
                        </xForms:SfListView.ItemTemplate>

                        <xForms:SfListView.DragItemTemplate>
                            <DataTemplate>
                                <FlexLayout BackgroundColor="#32EEEEEE" >
                                    <Label Text="{Binding Name}" TextColor="Black"/>

                                    <Frame HeightRequest="20" WidthRequest="20" CornerRadius="20" Padding="1" BackgroundColor="LightBlue">
                                        <Image Scale="0.7" Source="{Binding AssignedUser.Image}"/>
                                    </Frame>
                                </FlexLayout>
                            </DataTemplate>
                        </xForms:SfListView.DragItemTemplate>

                        <xForms:SfListView.FooterTemplate>
                            <DataTemplate>
                                <Button HeightRequest="45" Command="{Binding AddTaskCommand}" Text="{resources:Translate AddNewTask}"></Button>
                            </DataTemplate>
                        </xForms:SfListView.FooterTemplate>
                    </xForms:SfListView>
                </Grid>

            </popupLayout:SfPopupLayout.Content>

            <popupLayout:SfPopupLayout.PopupView>
                <popupLayout:PopupView AppearanceMode="TwoButton" AcceptButtonText="{resources:Translate Accept}" DeclineButtonText="{resources:Translate Cancel}" AcceptCommand="{Binding ConfirmPopupCommand}" HeaderTitle="{resources:Translate AddNewTask}" HeightRequest="150">
                    <popupLayout:PopupView.ContentTemplate>
                        <DataTemplate>
                            <Grid>
                                <Entry Text="{Binding NewTaskName, Mode=TwoWay}" Placeholder="{resources:Translate TaskName}" Keyboard="Text" TextColor="Black" HeightRequest="50"/>
                            </Grid>
                        </DataTemplate>
                    </popupLayout:PopupView.ContentTemplate>
                </popupLayout:PopupView>
            </popupLayout:SfPopupLayout.PopupView>

        </popupLayout:SfPopupLayout>

    </ContentView.Content>
</ContentView>