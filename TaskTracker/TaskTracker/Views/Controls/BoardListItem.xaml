<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xForms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:controls="clr-namespace:TaskTracker.Views.Controls;assembly=TaskTracker"
             x:Class="TaskTracker.Views.Controls.BoardListItem">
    <ContentView.Content>
        <Frame BackgroundColor="#FAFAFA" HasShadow="True" HeightRequest="60" Padding="1" Margin="20,0">
            <StackLayout>
                <Label HorizontalTextAlignment="Center" Text="{Binding Name}" TextColor="Black"/>

                <controls:AssignedUsersListView ItemsSource="{Binding AssignedUsers}" IsEnabled="false"/>
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>